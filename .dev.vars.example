# Local-only env for `wrangler dev` and `opennextjs-cloudflare preview`.
# Copy to .dev.vars and fill in. NEXT_PUBLIC_* are also read from .env.local
# at `next build` time.
GOOGLE_CLOUD_API_KEY=
SENTRY_AUTH_TOKEN=
